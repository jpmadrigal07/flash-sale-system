{
  "name": "@flash-sale/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig-build.json && mkdir -p dist/redis/scripts && cp src/redis/scripts/purchase.lua dist/redis/scripts/",
    "start": "node dist/index.js",
    "test": "vitest run",
    "lint": "eslint .",
    "stress": "tsx tests/stress/purchase-stress.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.0",
    "@flash-sale/shared": "*",
    "fastify": "^5.12.5",
    "ioredis": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "vitest": "^5.0.1"
  }
}
